# To-do-Application
Practice Project 1,
Uses Springboot, Spring MVC, Thymeleaf, JPA(hibernate), Lombok for boilerplate code, Java, MySql, Html, CSS, Bootstrap
adds tasks, removes tasks, toggles tasks by turning status of task from finished to not finished and vice versa.
